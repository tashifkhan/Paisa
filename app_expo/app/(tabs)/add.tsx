import React from "react";
import { View } from "react-native";

// This component is just a placeholder for the "Add" tab.
// The actual navigation is handled by the tabBarButton listener in _layout.tsx.
export default function AddScreen() {
	return <View />;
}
