# Paisa API URL (change for remote deployments)
PAISA_API_URL=http://localhost:8000

# Option 1: Provide a JWT token directly (get it from /auth/login)
PAISA_API_TOKEN=

# Option 2: Provide credentials and the server will authenticate on startup
PAISA_EMAIL=
PAISA_PASSWORD=
